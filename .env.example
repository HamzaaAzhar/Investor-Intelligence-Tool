# All variables are optional — sensible dev defaults apply if omitted.
# Copy to .env and fill in what you need.

FLASK_ENV=development
SECRET_KEY=dev-secret-change-in-prod
JWT_SECRET_KEY=dev-jwt-secret-change-in-prod
DATABASE_URL=sqlite:///investorlens.db
ADMIN_PASSWORD=admin123

# Required for the AI Insights panels (POST /api/ai-insight)
ANTHROPIC_API_KEY=
